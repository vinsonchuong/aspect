{
  "name": "aspect",
  "private": true,
  "workspaces": [
    "lib/*"
  ],
  "scripts": {
    "postinstall": "updatelibdefs",
    "test": "lint && checktypes && runtests",
    "start": "serve.js"
  },
  "dependencies": {
    "react": "^16.0.0"
  },
  "devDependencies": {
  },
  "engines": {
    "node": ">=8.1.0"
  }
}
